return {
  "echasnovski/mini.tabline",
  version = false,
  event = "VeryLazy",
  opts = {
    -- Set to true if you want each tab to represent a full Vim tab instead of buffers
    format = nil,
    show_icons = true, -- Shows filetype icons if you have a web-dev icons plugin installed
  },
}
